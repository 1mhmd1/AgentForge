# AgentForge — AI layer dependencies
# Install with: pip install -r requirements.txt

# LLM provider (Groq is default — fastest + free tier)
groq==1.2.0

# LangGraph — the state machine that runs the pipeline
langgraph==1.1.10
langchain-core==1.3.2

# Data validation — used to validate the planner's JSON output
pydantic==2.13.3

# Loads your .env file (API keys, model settings)
python-dotenv==1.2.2

# Template engine — used by the builder to generate agent code
jinja2==3.1.6
google-generativeai==0.8.3